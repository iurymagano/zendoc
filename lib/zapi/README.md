# lib/zapi/

Cliente HTTP da Evolution API (WhatsApp self-hosted, ~R$50/mês no Railway).

Será implementada na Sprint 2. Arquivos previstos (especificados em `CLAUDE.md`):

- `client.ts` — `sendWhatsAppMessage`, `createInstance`, `getConnectionStatus`, `getQRCode`
